services:
  omega-agent:
    build: .
    container_name: omega-agent
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ${VAULT_HOST_PATH:-./vault}:/app/vault
    environment:
      - OBSIDIAN_VAULT_PATH=/app/vault
